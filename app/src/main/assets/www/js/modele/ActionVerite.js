var ActionVerite = function(phrase, type, theme, difficulte, createur, id)
{
    this.id = id;
    this.phrase = phrase;
    this.type = type;
    this.theme = theme;
    this.difficulte = difficulte;
    this.createur = createur;
}